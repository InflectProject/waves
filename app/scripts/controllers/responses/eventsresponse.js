'use strict';

/**
 * @ngdoc function
 * @name wavesApp.controller:EventsresponseCtrl
 * @description
 * # EventsresponseCtrl
 * Controller of the wavesApp
 */
angular.module('wavesApp')
  .controller('EventsResponseCtrl', ['$stateParams', function ($stateParams) {
      $stateParams;
    }]);
