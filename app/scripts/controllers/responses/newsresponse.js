'use strict';

/**
 * @ngdoc function
 * @name wavesApp.controller:NewsresponseCtrl
 * @description
 * # NewsresponseCtrl
 * Controller of the wavesApp
 */
angular.module('wavesApp')
  .controller('NewsResponseCtrl', ['$stateParams', function ($stateParams) {
      $stateParams;
    }]);