'use strict';

/**
 * @ngdoc function
 * @name wavesApp.controller:WeatherresponseCtrl
 * @description
 * # WeatherresponseCtrl
 * Controller of the wavesApp
 */
angular.module('wavesApp')
  .controller('WeatherResponseCtrl', ['$stateParams', function ($stateParams) {
      $stateParams;
    }]);
